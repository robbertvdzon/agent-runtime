apiVersion: v1
kind: Service
metadata:
  name: agent-runtime-server
  labels: {app: agent-runtime-server}
spec:
  selector: {app: agent-runtime-server}
  ports:
    - {name: http, port: 80, targetPort: http}
